const SERVER_PORT = 8080;
const STATIC_IMAGE = "public/images";
const IMAGE_URL_PREFIX = `http://127.0.0.1:${SERVER_PORT}/${STATIC_IMAGE}/`;

module.exports = {
    SERVER_PORT,
    STATIC_IMAGE,
    IMAGE_URL_PREFIX
}